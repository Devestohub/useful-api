'use strict';

module.exports = {
    Langs: require('./langs'),
    Roles: require('./roles'),
    Users: require('./users'),
    Update: require('./update'),
    version: require('../package.json').version
}
'use strict';

module.exports = {
    Crypt: require('./crypt'),
    Langs: require('./langs'),
    Roles: require('./roles'),
    Users: require('./users'),
    version: require('../package.json').version
}